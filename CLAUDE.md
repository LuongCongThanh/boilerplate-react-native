# CLAUDE.md

Hướng dẫn cho Claude Code khi làm việc với repo này.

## Agent skills

### Issue tracker

Issues được quản lý trên GitHub Issues của repo `LuongCongThanh/boilerplate-react-native`. Xem `docs/agents/issue-tracker.md`.

### Triage labels

Dùng 5 nhãn mặc định (needs-triage, needs-info, ready-for-agent, ready-for-human, wontfix). Xem `docs/agents/triage-labels.md`.

### Domain docs

Single-context — một `CONTEXT.md` và `docs/adr/` tại thư mục gốc. Xem `docs/agents/domain.md`.
